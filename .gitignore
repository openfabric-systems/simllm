# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Simulation outputs (keep bulk artifacts out of the repo)
runs/
*.goal.bin
*.bin
*.npz
*.csv
!tests/data/*.csv
!examples/rnic_wq_v1/results.csv

# Local agent rule files (kept out of the public repo on purpose)
CLAUDE.md
AGENTS.md

# Editors
.vscode/
.idea/
*.swp

# Downloaded reference papers (bulk PDFs at the repo root; the
# committed summaries live in docs/papers/)
/papers/

# Local bottom-layer records (decision notes and explorative studies;
# see the two-layer rule in AGENTS.md)
/notes/
