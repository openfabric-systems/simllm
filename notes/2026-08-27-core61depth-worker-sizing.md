# CORE-61 depth-validity worker sizing

## CORE-61 local separated-extrapolation arm

- As-of commit: `97d34b0aa34fb25f55c24cfd39b923119cf69c95`.
- Scope: freeze the expected signed effect of separating per-step fixed service
  from four-layer per-layer service, read only the predeclared fields from the
  retained COMP-64-class EP72 standard-decode decomposition, and derive the
  corresponding declared 61-layer step without fitting to published
  throughput.
- Hypothesis: the existing linear rule `T(61) = 61 / 4 x T(4)` multiplies each
  per-step fixed component by 15.25. If the retained decomposition supports
  `T(4) = F + 4 x p` with `F > 0`, then `T(61) = F + 61 x p` is strictly
  smaller than the existing linear projection by `14.25 x F`.
- Assumptions: the retained candidate's own component labels and repetition
  scopes are authoritative for this local derivation; no component is
  reclassified from its recorded semantics; and the published 22,282
  tokens-per-second calibration value is comparison context only.
- Exclusions: no scored-run edits, no analytical-gate work, no decode session
  code, no NVLink work, no COMP-76 movement, no model-weight download, no web
  access, no deletion, and no README prose beyond permitted mechanical task
  progress or open-count cells.
- Owner: CORE-61 Codex worker on `codex/core61_depth_validity` in worktree
  `core61d`.
- Dependencies: the retained four-layer EP72 standard-decode decomposition,
  the TRAF-67 field-addressed exposure protocol, and a second measured depth on
  Merlin after the maintenance window beginning no earlier than
  `2026-08-28T06:30`.
- First reviewable slice: an expectations-only freeze that records the exact
  permitted field paths, access-log contract, fixed-versus-per-layer
  classification rule, signed hypothesis, historical artifact locks, and the
  exact deferred capture commands before any retained-record field is read.

### Expected files

- Created: this sizing note plus a compact CORE-61 expectations freeze,
  field-addressed access ledger, derivation result, and maintainer report.
- Created or modified after the freeze: a narrowly scoped local derivation
  helper and focused tests, the CORE-61 registry wording, and COMP-72's
  resumable Merlin remainder only as literal acceptance permits.
- Preserved: all scored runs, analytical-gate artifacts, decode session code,
  the NVLink module, COMP-76, and every retained source record.
- Bulk evidence: any later capture output belongs under the task-owned external
  root named by `SIMLLM_CORE61_RUN_ROOT`; the expectations JSON pins its exact
  value and the output is not tracked.

### Expected handwritten line ranges

- Production or reusable study code: 80 to 220 lines.
- Focused tests and compact fixtures: 120 to 320 lines.
- Freeze, access ledger, result, registry, and handoff documentation: 220 to
  500 lines.
- Deferred Merlin capture output and other bulk evidence: counted as zero
  handwritten lines.

### Confidence and uncertainty

- Confidence: medium that the retained decomposition exposes a nonzero
  per-step fixed component with an unambiguous repetition scope.
- Dominant uncertainty: whether the retained record separates every component
  needed for a complete four-layer reconstruction rather than only a subset of
  kernel service. A null or incomplete separation will be published plainly.
- Closure rule: land the single-depth declared derivation and keep CORE-61 open.
  Acceptance still requires a frozen held-out prediction within 5 percent of a
  second measured depth plus the signed residual ledger. That capture joins the
  resumable COMP-72 Merlin remainder; COMP-76 remains untouched.
