# CORE-54 fourth scored flagship worker sizing

## CORE-54 MTP anchor run

- As-of commit: `93e877d4a6d4cc76875579249c28e6fd8c64421c`.
- Scope: preregister the MTP step-to-token accounting, 61-layer depth
  treatment, per-GPU batch-16 mapping, decode-side attenuation ruling and all
  applicable prediction bands before computing a scored value. Then run the
  disclosed simulated-MTP configuration once, score only
  `sglang_decode_simulated_mtp`, carry the third-run 2K and 4K verdicts forward
  unchanged, render the updated flagship figure and publish a new
  content-addressed record.
- Assumptions: the merged LF pin at the base commit makes the partial Hopper
  campaign successor record the evidence authority; its MTP decode cell is a
  measured four-layer step with two independent observations and a disclosed
  component overlay; the run-3 calibration-only fit remains frozen unless the
  expectations commit explicitly declares otherwise.
- Exclusions: no model-weight download, no web access, no held-out value access
  before the frozen fit, no envelope widening, no reuse of the EP32 prefill
  attenuation factor without an independent EP72 decode derivation, no
  deletion, no remote Git mutation and no README prose outside mechanical task
  progress or open-count cells.
- Owner: CORE-54 Codex worker on `codex/core54run4_mtp` in worktree
  `core54run4`.
- Dependencies: the three scored-run lineage, the frozen benchmark-bias model,
  the pinned SGLang speculative-accounting source, the merged
  `d868a4f3...` successor evidence, CORE-61's open depth-linearity question and
  the deployment-frontier publication contract.
- Reserved identifiers: `CORE-63` and `COMP-79`; this run will not allocate
  either identifier to another concern.
- First reviewable slice: a field-addressed record reader and access log,
  committed before any permitted evidence-field read. The next slice is the
  expectations-only freeze. No score may be computed before both commits.

### Record-access chronology

- Before this sizing note and before a run-4 field reader existed, an initial
  broad lineage search used `rg` across `docs`, `src`, `tests`, `notes`,
  `records` and the repository root. It crossed into `examples/` and surfaced
  snippets from prior study material. This breached the required clean
  field-reader-first chronology.
- No run-4 prediction, fit or score was computed, and the output did not expose
  the held-out MTP anchor beyond the value already stated in the task brief.
  The breach cannot be undone. It will remain disclosed in the freeze, access
  log and final report.
- From this point, record values will be read only through the committed
  run-specific field-addressed reader. Every permitted field address and its
  purpose will be appended to the run-specific access log. Whole-file record
  reads remain forbidden.

### Expected files and size

- Created first: this sizing note, about 75 to 120 lines.
- Expected expectations layer: one compact JSON freeze, one Markdown freeze
  explanation, one field-addressed reader, one append-only access log and
  focused freeze tests, about 650 to 1,200 handwritten lines.
- Expected execution layer: one run-specific scorer that reuses the run-3 fit
  machinery, one exact disclosed MTP configuration, one focused runner test
  module and an external attempt directory under `$SIMLLM_CORE54RUN4_RUN_ROOT`,
  about 450 to 900 handwritten lines. Dispatch binds that variable to the
  requested external `wave-runs/core54run4` directory.
- Expected publication layer: one compact result JSON, one content-addressed
  record, one report, one figure data table, one updated flagship PDF and PNG,
  a deployment-frontier update only if its freeze permits it, and focused
  publication tests, about 450 to 900 handwritten lines. Generated JSON and
  figure binaries count as zero handwritten lines.
- Expected registry layer: the literal CORE-54 row and mechanical task/open
  counts only if warranted by all three scorable held-out rows. Open residuals
  will name decode calibration reproduction, COMP-74 distribution propagation,
  the Granite campaign arm and depth linearity.

### Validation and completion rule

- Before every local commit, verify LF behavior with `git check-attr`, run Ruff
  from the worktree Python 3.10 virtual environment and run the full pytest
  suite with pytest's own exit status checked directly, never through a pipe.
- Every text writer added by the run will pass `newline="\n"`. Production code
  will avoid Unix-only module-scope imports and `typing_extensions`.
- Every prior preservation-lock artifact, now at least 43 artifacts, must stay
  byte-identical. The run publishes one new content-addressed record and never
  modifies an earlier result or record.
- The only new held-out access is the one-time MTP anchor read after the frozen
  prediction is serialized. A miss is published without changing any band,
  fit, factor or mechanism.
- Confidence is medium-high for deterministic arithmetic and lineage reuse.
  The dominant uncertainty is whether four-layer measured service extrapolates
  linearly to 61 layers and whether simulated-MTP emission accounting closes
  the 5 percent bar without an admissible decode attenuation factor.

### Completion accounting

- The field boundary, expectations freeze and one-shot scorer landed in five
  reviewable commits before publication work began.
- Attempt 1 was the only scored attempt. It realized 128 requests, 16 on each
  GPU, emitted 256 simulated-MTP tokens per node step and returned the honest
  52.493305 percent-low refutation in all three frozen layers.
- Publication adds one compact content-addressed JSON record and SHA-256
  sidecar, one nine-row CSV, one Markdown report, one PDF, one PNG, two small
  publication programs and one focused result-lock test module. No previous
  artifact changed.
- The deployment-frontier figure was not refreshed because its frozen v2
  contract has no MTP marker slot and its existing bytes are preservation
  locked.
- The final record retains the chronology breach above, the one held-out
  access, the one score attempt and the later field-addressed publication
  projections. No weights or web pages were fetched.
