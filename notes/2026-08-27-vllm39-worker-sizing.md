# VLLM-39 load-delay worker sizing

Date: 2026-08-27
Branch: `codex/vllm39_load_delay`
Base: `97d34b0aa34fb25f55c24cfd39b923119cf69c95`

## Scope

Identify the load-delay shape of the concurrent vLLM disaggregated session by
pricing its decode pool from the merged Granite batch-service candidate. Freeze
the offered-load range, held-out prompt and pool-ratio families, selected
surface keys, queue/service decomposition, signed segment directions and
held-out prediction bands before running the study.

The lane is limited to the `pd_session_concurrent` lineage and the vLLM adapter
documentation. The parallel analytical-gate and `deployment_curve_v1` work,
plot machinery, model-weight downloads, web access, remote Git mutation,
deletion and README prose outside mechanical task-progress or open-count cells
are excluded. Bulk run evidence belongs under `$SIMLLM_VLLM39_RUN_ROOT`;
dispatch binds that variable to the requested external `wave-runs/vllm39`
directory.

## Exposure protocol and incident

- The required clean record-exposure order was breached during initial
  reconnaissance. A broad repository `rg` command scanned files under
  `examples/hopper_kernel_cycle_candidate_v1` before a field-addressed reader
  was committed and surfaced a DeepSeek summary row.
- No value exposed by that command may inform the freeze, model, prediction
  bands or verdict. The exact command and affected path family will be retained
  in the study access ledger.
- All subsequent candidate-record access must use the committed TRAF-67-style
  field-addressed reader, select only the frozen Granite decode batch keys and
  append an access event for every requested field.
- Candidate records must never be read as a whole. DeepSeek rows and the
  held-out-shape row family remain forbidden.
- Because the pre-reader scan makes this attempt exposure-contaminated,
  VLLM-39 cannot claim a clean close unless its literal registry entry permits
  that disclosed history. Otherwise the clean repetition is registered on a
  reserved residual ID.

## Estimated change

Expected tracked work is one compact expectations-only freeze, a reusable
field-addressed reader and access ledger, a concurrent-session surface-pricing
extension, focused freeze/result tests, compact curve records and a Markdown
result. Registry changes will be limited to literal task state and permitted
mechanical progress cells. The CORE-51 one-request control, deterministic
comparator and every scored flagship artifact remain preservation locks.

## Commit plan

1. Land the exposure reader and expectations-only freeze without accessing any
   permitted candidate value.
2. Read only the frozen Granite keys through the landed reader, execute the
   frozen concurrent sweep and publish conservation, segment-direction, knee
   and held-out-band verdicts.
3. Reconcile VLLM-39 and VLLM-35 literally, register any clean-repetition or
   model residual on VLLM-40 or VLLM-41, prove preservation and run every
   required validation gate before each local commit.
