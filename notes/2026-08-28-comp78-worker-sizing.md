# COMP-78 campaign-remainder worker sizing

## Literal COMP-72 remainder

- As-of commit: `36c70b4cf41d4e32825de3e0d57024eda5a43067`.
- Scope: submit the frozen CORE-61 depth-8 base and decode captures in order,
  score the decode measurement against the preregistered 3,751,359,511 ps
  prediction with a signed residual, stage pinned real Granite framework
  targets, resume the canonical 1,212-cell campaign without overwriting any
  attempt, and compile the evidence-backed content-addressed successor.
- Priority: CORE-61 depth-8 first, then the polite Granite completed prefix,
  then the successor and registry movement supported by retained evidence.
- Assumptions: the integrator's `2026-08-28T06:30` Europe/Zurich gate verdict
  is authoritative; the Merlin MFA session and existing remote campaign roots
  remain available; and hourly partition occupancy permits only short paced
  submissions.
- Exclusions: no expectations amendment, scored-run change, model-weight
  download, web fetch, deletion, deployment work, p2loggopsim work, README
  prose outside permitted mechanical task progress, or overwrite of a
  completed or failed attempt.
- Owner: COMP-78 Codex worker on `codex/comp78_campaign_remainder` in worktree
  `comp78`.
- Dependencies: the immutable `ff46f6d8...` predecessor and `d868a4f3...`
  partial successor, the canonical `19a6d6fb...` Granite plan, the pinned
  framework installations already present on Merlin, and the landed campaign
  compiler.
- First reviewable slice: retained digest-complete CORE-61 base and decode
  outputs plus the signed prediction residual, before any Granite submission
  or successor publication.

### Expected files

- Created: this sizing note and a compact COMP-78 result or handoff report.
- Modified after retained evidence exists: CORE-61 result and ledger files,
  campaign evidence and compiler inputs, generated successor artifacts, and
  the COMP-72, COMP-78, and CORE-61 registry wording as literal acceptance
  permits.
- Preserved: all scored freezes and results, both historical lookup records,
  every existing failed or completed campaign attempt, integrator-owned README
  prose, and files owned by the deployment and p2loggopsim lanes.
- Bulk evidence: remote output remains below `~/simllm-data/campaign` on Merlin
  and task-local output remains below
  `/data3/yifeng/simllm-dev/wave-runs/comp78`; only lean digest-bearing evidence
  is pulled into the worktree-facing compilation root.

### Expected handwritten line ranges

- Campaign and CORE-61 evidence compiler changes: 100 to 320 lines.
- Focused tests and compact fixtures: 120 to 360 lines.
- Result, ledger, registry, and handoff documentation: 180 to 420 lines.
- Generated successor JSON and external campaign evidence: counted as zero
  handwritten lines.

### Confidence and uncertainty

- Confidence: high that the frozen depth-8 jobs can be submitted exactly and
  scored once their digest-complete outputs are retained.
- Dominant uncertainty: hourly GH200 occupancy and whether pinned real vLLM
  and SGLang entry points can execute all 1,212 Granite cells inside polite
  windows without the MFA session expiring.
- Closure rule: close COMP-78 and COMP-72 only if all 1,212 Granite cells and
  both CORE-61 captures are digest-complete with the signed residual and every
  source/output digest retained. Otherwise publish the honest partial and the
  exact first resume cell. COMP-79 is already allocated in the checked-out
  registry, so do not overwrite it; keep COMP-78 open until the integrator
  assigns another available remainder ID.
