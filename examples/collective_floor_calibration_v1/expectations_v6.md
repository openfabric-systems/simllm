# Collective completion qualifying-candidate freeze

This sixth freeze precedes implementation and evaluation of the qualifying
candidate. The arithmetic symmetric candidate in the fifth freeze was
explored locally before its freeze and therefore remains post-specified
regression evidence. Its attempt-0007 63-of-63 result does not enter the
qualifying denominator.

The qualifying candidate changes exactly one operator. Where the fifth freeze
takes the arithmetic mean of the same-operation affine training value and the
complementary operation's exact training anchor, this candidate takes their
geometric mean. The geometric mean is the natural center for the multiplicative
operation ratio that attempt 0005 exposed, without applying that broad ratio
as a correction.

All other fifth-freeze rules remain exact:

- exact same-operation training anchors remain unchanged;
- a complementary exact trough below both of its own adjacent training
  anchors remains unchanged;
- reduce-scatter at no more than 8 KiB takes the maximum of the symmetric
  value, same-operation affine value and complementary all-gather anchor;
- the independent 0.78 Hopper transition fraction applies to the same two
  declared all-gather transition regions and terms;
- off-grid coordinates, including D8 at 196,608 bytes, retain
  same-operation affine interpolation;
- the authority is an opaque fully local completion charged once, while the
  floor-plus-slope compatibility transfer remains byte-identical.

No Family H latency supplies a coefficient, threshold, floor or branch. The
63 training cells, 63 holdouts, larger of 10 percent or two-cycle H band and
`[0.90, 1.10]` D8 band do not change. Family H is evaluated once after the
serialized qualifying authority is hashed. All 63 must pass. A miss refutes
the candidate. Every fourth-, fifth-, arithmetic- and qualifying-candidate
error remains visible, but only the qualifying candidate enters this freeze's
63-cell denominator.

All fatal guards from the fourth and fifth freezes remain binding. Physical
sanity remains ring endpoint bytes divided by 450 GB/s as the floor, with no
finite algorithm-progress ceiling supplied by the source.
