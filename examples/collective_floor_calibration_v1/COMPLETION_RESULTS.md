# Collective floor completion results

What ran: attempt 0009 evaluated the frozen qualifying aggregate anchor in two
fresh processes over all 63 Family H cells, the matched D8 coordinate, the
pre-wave bypass and all 16 published MiniMax legacy queries, then applied the
packet feasibility guards to the eight frozen PZ and PN cells.

What came out: Family H passes 63 of 63 with 2.5010 percent median, 8.6958
percent p95 and 9.9262 percent maximum relative error. D8 passes at quotient
0.946736591. The packet family is undecidable and has no denominator because
the H200 product parameters and prerequisite packet structure are not
identified.

What it changes: Legs A and B of TRAF-76 are complete. TRAF-76 narrows to the
packet integration and remains open under the closure rule. TRAF-82 owns that
residual packet work and its hardware evidence campaign.

What it does not change: the packet mechanism is not implemented, no packet
accuracy claim is made, and the nonzero-fan-in path remains an explicitly
transferred local component. The accepted default-off behavior and all
published MiniMax donor transfers remain byte-identical.

## Chronology

Attempt 0004 is the corrected predecessor. It passed 51 of 63 Family H cells
and refuted D8 at quotient 1.109143050.

Attempt 0005 froze the paired-operation trend ratio before repository
implementation. It is retained as a refutation at 46 of 63. The ratio
amplified broad operation differences and erased local troughs. Its complete
63-cell ledger remains in [ATTEMPT_0005.md](ATTEMPT_0005.md).

Attempt 0006 first used the arithmetic second-candidate freeze and remains append-only
bulk evidence. Its numbers are unchanged, but its publication input digest was
superseded when the retained attempt-0005 note corrected the D8 physical floor
from 0.341333 us to 0.382293 us.

Attempt 0007 repeats the arithmetic candidate after the retained note
correction. Both arithmetic results are post-specified regression evidence and
do not enter the qualifying denominator.

Attempt 0008 is the first qualifying geometric evaluation. Attempt 0009 is the
current publication with explicit chronology metadata. Its authority was
serialized and hashed before holdout loading. Both fresh-process records match after
excluding only `wall_time_seconds`, and every shared fatal guard held. The
machine-readable record is [completion_record.json](completion_record.json),
with the 63-cell table in
[completion_results.csv](completion_results.csv).
Their SHA-256 digests are
`f784eeb48fdf06cf7e7f7814d2d3554b9394307a0e0311a0c913522f2315f3d5`
and `2b224e12da10558d1e128560aa4eeae008fb019dcd4302c3a32396401d0e4e08`.

## Leg A: Family H

The 12 attempt-0004 misses sat at ranks 2, 4 and 8, across both operations and
8 KiB through 32 MiB. They did not form one universal dip. Three all-gather
misses did share the 4 MiB coordinate, and the rank-8 middle region contained
the separate 256 KiB reversal. The complete location map is
[MISS_MAP.md](MISS_MAP.md).

The completed authority removes the attempt-0005 broad trend ratio. Its
ordinary value is the geometric mean of the requested operation's local
training interpolation and the complementary operation's exact same-byte
training anchor. A complementary training trough remains a trough instead of
being averaged away. Reduce-scatter at no more than 8 KiB cannot undercut the
same-byte gather transfer floor. The two mapped all-gather transition regions
use the independent Hopper result that bandwidth retains 78 percent through
the transition. No H200 holdout latency supplies a coefficient.

All 63 cells meet the unchanged 10 percent or two-cycle bar. The worst cell is
rank-8 reduce-scatter at 524,288 bytes, at 9.9262 percent. The median is 2.5010
percent and p95 is 8.6958 percent. Every measured and predicted completion is
above ring endpoint bytes divided by 450 GB/s. The source identifies no finite
algorithm-progress ceiling, so the honest ceiling is unbounded.

The opaque completion is charged once and exposes zero serialization service.
The old floor-plus-slope object remains the byte-identical explicit-transfer
authority. All 16 dense and sparse MiniMax queries reproduce their published
floor, slope, evidence class and transfer reason exactly.

## Leg B: D8

The mechanism was broad model-form bias, not a byte-coordinate error. At the
unchanged 196,608-byte operation-buffer coordinate, same-operation training
interpolation gives 13.186667 us for reduce-scatter and 14.808334 us for
all-gather per layer. Across 65 layers that is 1.819675065 ms against the
unchanged 1.922050 ms external arm, quotient 0.946736591 inside `[0.90, 1.10]`.
No D8-specific constant or band change is present.

The per-phase physical floor is 0.382293 us from ring endpoint bytes divided
by 450 GB/s. Both modeled contributions are above it.

## Independent sanity angles

Network physics puts every H and D8 prediction above endpoint bytes divided by
450 GB/s. Mechanism shape is checked independently by the published A100 and
GH200 transition result: only the mapped transition terms use its fixed 0.78
bandwidth fraction, while ordinary cells do not. End-to-end plausibility is
checked by the unchanged 1.922050 ms D8 external arm and by the 16 published
MiniMax dense and sparse queries, all of which retain their accepted legacy
transfer values exactly.

## Leg C: packet mechanism

The frozen matrix contains four zero-fan-in and four nonzero-fan-in cells over
two payloads. It did not run. PC-FG-1 and PC-FG-2 are undecidable: the source
has no independent H200 credit, queue, port, switch or arbitration values, and
the tree lacks the generation-scoped flit, receiver-owned credit, virtual
channel, replay, receive-order, input-port, virtual-output-queue and two-sided
crossbar structure required by the freeze.

No before or after phase-completion error exists, and no scored denominator is
reported. The one-authority proof is that no packet path is enabled, leaving
the opaque aggregate row as the sole timing owner. The no-double-count proof
is that the unexecuted packet family contributes zero bytes, service, queue
wait and aggregate charge. The detailed successor design is
[the TRAF-82 packet boundary](../../docs/design/traf82-h200-packet-collective.md).

## Preservation and artifacts

The generated pre-wave scenario reproduces every phase and step timestamp,
local and fabric segment, application and wire byte count, completion order,
backend invocation and random-generator state. Two fresh evaluations match
exactly apart from wall time. The append-only bulk evidence is selected by the
runner's `--workdir` argument and is not named by a machine-specific tracked
path.

## Adversarial review correction, 2026-09-01

This append-only section controls the interpretation of the earlier
classification and consequence language. It changes no frozen expectation,
configuration, runner, result row, digest, band or fitted number.

### Family H evidence classification

The 63-of-63 result is a post-specified regression on an adaptively reused
63-cell evaluation set with training-cell-only numeric evaluation. The
training and evaluation identities stayed disjoint, and no evaluation value
was read while any one candidate was numerically fitted. That mechanical fact
does not make the result an untouched holdout qualification. Across the
attempt sequence, the same 63 evaluation cells informed selection of the
geometric operator, trough preservation, the 8 KiB reduce-scatter floor, the
transition coordinates, and the rank-8 256 KiB and 4 MiB branches.

The honest result is therefore post-specified 63 of 63, up from the prior 51
of 63, with unchanged median, p95 and maximum errors of 2.5010, 8.6958 and
9.9262 percent. Words such as `qualifying`, `holdout` and `qualification` in
the immutable freezes, configuration, runner and machine record name their
original chronology and mechanical data split only. They do not claim
independent validation. TRAF-84 owns genuinely independent H200 validation on
cells never used in fitting or any selection decision.

### MiniMax successor consequence

The existing MiniMax EP 8 PASS holds only under the legacy authority pin. The
successor surface evaluates the exact consumed coordinate: the same two
operations at rank 8, 196,608 operation-buffer bytes, 65 repeats and the same
1.922050 ms external arm. Explicit successor binding produces quotient
0.946736591 against the MiniMax freeze's packet-over-external requirement of at
least 1.0, so it refutes EP 8.

Under successor binding, MiniMax Family D is 0 of 3. EP 32 and EP 128 stay
refuted as rank-8 donor transfers that delegate to the legacy authority; the
successor authority rejects their unfitted ranks. The 16 exact reproductions
remain valid only as a legacy-preservation proof. No default binding changes in
this correction. TRAF-83 registers the explicit rebinding decision and the
required superseding MiniMax publication with all bands unchanged.

### TRAF-76 consequence

Leg B remains complete at quotient 0.946736591 inside its unchanged
`[0.90, 1.10]` matched-coordinate band. Leg C remains honestly undecidable,
with no scored denominator or before-and-after error. Leg A retains real
post-specified regression progress from 51 of 63 to 63 of 63, but it is not an
independent qualification. TRAF-76 remains open for the packet integration in
TRAF-82, MiniMax successor rebinding in TRAF-83 and independent H200 validation
in TRAF-84. Nothing in this correction changes packet coverage, legacy bytes,
timestamps, completion order, backend invocation order or random draws.
