# CORE-63 clean registry block freeze

Status: **EXPECTATIONS ONLY FOR REGISTRY BLOCK RESOLUTION**. The first reverse
resolver correctly identified the entry-leading CORE-63 and CORE-64 lines but
stopped CORE-64 at an internal blank line. This bounded extension retains blank
continuations and stops at the next entry-leading CORE identifier or Markdown
section heading.

Exactly two reverse-tail accesses and four contemporaneous events are
permitted. The terminal byte remains unread and complete unique-byte coverage
is rejected. This syntax-only resolution is not an arithmetic input and cannot
change the clean undercorrection, preservation locks, empty forbidden ledger,
or MTP no-read status.
