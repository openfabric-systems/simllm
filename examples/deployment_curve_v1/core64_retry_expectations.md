# CORE-64 structural-label retry expectations

Status: **EXPECTATIONS ONLY FOR STRUCTURAL RETRY**. This freeze precedes the
retry and changes no shape arithmetic, component classification, scale,
expected direction, or registry acceptance rule.

## Retained first tranche

The committed reader completed all three partial selectors with six
contemporaneous events. Each END status is PASS, every byte count is below its
source size, the standard-case selector stopped before the MTP case, and the
forbidden-access ledger remains empty. The arithmetic stage then rejected the
projection's literal attention-parallelism label because the runner expected
the wrong spelling. It emitted no result and computed no movement.

The first ledger remains append-only evidence. The retry uses a new ledger and
the identical three selectors. The attention label must be a nonempty string;
DP72 authority remains the already frozen SGLang arrangement and the selected
standard case must still report exactly 32 new tokens per rank. The retry may
not weaken any numerical or component check.
