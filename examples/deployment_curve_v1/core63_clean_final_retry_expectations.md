# CORE-63 clean final access freeze

Status: **EXPECTATIONS ONLY FOR FINAL ACCESS RETRY**. This final access plan is
committed before it runs. It does not amend the arithmetic, component rule,
expected direction, or registry acceptance.

## Preserved safe rejections

Both prior attempts remain immutable, contemporaneously logged preflights.
Each performed two partial JSON projections and then rejected the CSV selector
at 13,984 of 13,985 bytes. Neither read the final byte, reported a whole-file
stream, printed or persisted a basis, or accessed a held-out MTP value. The
forbidden-access ledger remains the exact empty array.

## Final field-addressed byte plan

The catalog freezes this CSV as the legacy single-device schema. The final
reader therefore does not stream or decode the header. It starts at byte
13,983 and reads backward through byte zero, synthetically terminating the
last selected row without accessing byte 13,984. The selector can access
exactly 13,984 unique bytes, strictly fewer than the 13,985-byte record, and
the reader still rejects any attempt at complete unique-byte coverage.

All rows remain field classified under the original allowlist. The reader
restores their forward order before applying the unchanged
`fused_moe_kernel` marker. This is the last access attempt. Across the two
safe preflights and the final six-selector tranche, the evidence will contain
twelve accesses and twenty-four contemporaneous events, with zero whole-file
streams and an empty forbidden ledger.
