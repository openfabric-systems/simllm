# CORE-65 physical-binding access protocol

Status: **EXPECTATIONS ONLY, PRE-READER INCIDENT RECORDED**. This protocol,
its reader, its signed directions, its forbidden-access ledger, and its
preservation manifests must be committed before any further protected record
access.

## Access boundary

The reader permits only these selectors:

1. `component_basis` and `kernel_rows` in the retained CORE-63 bulk basis at
   `<wave-runs>/core63/reproduction/basis-success.json`.
2. The standard-decode calibration, retained physical decomposition,
   preservation result, registry disposition, and scope in
   `<repo>/examples/deployment_curve_v1/core64_shape_result.json`.
3. `geometry_symbols`, `inventory_contract`, `deployment_projection_contract`,
   and `physical_sanity` in the expectations-only DeepSeek-V3 extraction
   record.
4. The literal CORE-65 registry block in `<repo>/docs/modules/core.md`.
5. If it is still available, `model`, `tensor_parallel_size`, `mode`,
   `deepseek_suite`, `reduced_layers`, `model_config`, and the
   `decode_b32_c2000` cell in
   `<kernelprobe-root>/gh200lane/capture-198891-deepseek-v3-tp1-graph-decode/profile.json`.

Every access writes a `BEGIN` event before source open and an `END` event
immediately after selection. The reader binds each tracked or retained bulk
record to its frozen byte size. The optional original profile logs a rejected
access if it is unavailable.

## Whole-file rejection and forbidden boundary

All JSON and Markdown sources use byte-counting guards that reject access one
byte before complete coverage. Top-level selectors return at the closing byte
of the selected value. The registry selector reads sparse reverse lines while
leaving the terminal record byte untouched. No protected reader prints an
unselected field or persists a whole-record basis.

The held-out MTP numeric value, the second EP72 decode case, every fifth-run
scoring artifact, and all MTP comparisons are forbidden. A pre-reader broad
search exposed a published MTP row and a broad registry search streamed the
registry without a contemporaneous access row. These incidents are recorded
without reproducing the numeric value in `core65_forbidden_access_ledger.json`.
The ledger is therefore not empty and this worker cannot claim literal
protocol closure. The exposed value is not an arithmetic input and must not be
compared.

## Frozen physical-binding rules

The candidate-specific signed directions and component-specific scale rules
are frozen in `core65_expectations.json` and `core65_expectations.md`. Kernel
names may be grouped only by an explicit, documented physical identity rule.
No service may be moved by an expert, layer, or byte scale unless the retained
evidence identifies that kernel's tracking variable. Opposing dense-only and
MoE-only layer-composition terms remain separate until their services are
known. No fitted or free calibration constant is permitted.

## Publication and preservation

Publication is calibration-only against the existing 22,282 tokens/s/node
standard-decode anchor. The held-out MTP value remains unused and uncompared.
The inherited 134-file class plus 20 CORE-64 files form a 154-file minimum
preservation class. Hash verification may decode no protected artifact value.
The fifth scored run and parallel-owned deployment and traffic lanes remain
untouched.
